[project]
name = "stacks-phantom-record-matrix"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/stacks-phantom-record-matrix/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.stacks-phantom-record-matrix]
path = "contracts/stacks-phantom-record-matrix.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
